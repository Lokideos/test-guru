<section class="default-form">
  <div class="row justify-content-center">
    <div class="col col-md-4">
      <h2 class="default-header"><%= @badge.name %></h2>
    </div>
  </div>

  <div class="row justify-content-center">
    <div class="col col-md-6">

      <%= render 'form', badge: @badge %>

      <div class="mt-1">
        <%= link_to t('common.back'), admin_badges_path %>
      </div>
    </div>
  </div>
</section>