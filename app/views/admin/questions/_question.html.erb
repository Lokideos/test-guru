<tr>
  <th><%= question.body %></th>
  <th><%= link_to t('common.show'), admin_question_path(question) %> |</th>
  <th><%= link_to t('common.edit'), edit_admin_question_path(question) %> |</th>
  <th><%= link_to t('.destroy'), 
                  admin_question_path(question),
                  method: :delete,
                  data: { confirm: t('data.messages.delete_confirmation') } %>
  </th>
</tr>