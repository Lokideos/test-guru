<section class="default-form">
  <div class="row justify-content-center">
    <div class="col col-md-4">
      <h2 class="default-header"><%= t('admin.tests.show.header', title: @question.test.title) %></h2>
    </div>
  </div>

  <div class="row justify-content-center">
    <div class="col col-md-6">
      <h3><%= question_header(@question) %></h3>

      <%= render 'form' %>

      <div class="mt-1">
        <%= link_to t('common.back'), admin_test_path(@question.test) %>
      </div>
    </div>
  </div>
</section>