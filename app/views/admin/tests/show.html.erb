<h1><%= t('.header') + " " + @test.title %></h1>

<p><%= render @test %></p>

<p><%= link_to t('.create_new_question'), new_admin_test_question_path(@test) %></p>

<table>
  <tr>
    <tr>
    <th><%= t('.questions_body') %></th>
    <th colspan="3"><%= t('.actions') %></th>  
  </tr>

  <%= render @test.questions %>
</table>

<p><%= link_to t('common.edit'), edit_admin_test_path(@test) %></p>
<p><%= link_to t('common.back'), admin_tests_path %></p>